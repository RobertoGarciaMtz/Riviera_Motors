@model IEnumerable<Riviera_Business.Models.CConcepto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionConcepto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionConcepto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCConcepto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCConcepto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCConcepto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
